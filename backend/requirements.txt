# Core — pinned for stable deployment
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database — asyncpg (async, works with RLS set_config)
asyncpg==0.29.0

# Pydantic v2
pydantic==2.7.1
pydantic-settings==2.2.1

# Anthropic SDK
anthropic==0.26.0

# HTTP client for OTA push
aiohttp==3.9.5

# Env
python-dotenv==1.0.1

# Starter repo listed these — kept for compatibility
sqlparse==0.5.0
pgvector==0.3.0

# Test HTTP client
httpx==0.27.0
